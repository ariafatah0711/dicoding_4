const Home = {
  async init() {
    await this._initialData();
    this._initialListener();
  },

  async _initialData() {
    // console.log("init listener Home");
  },

  _initialListener() {
    // console.log("init listener Home");
  },
};

export default Home;
