const Logout = {
  async init() {
    await this._initialData();
    this._initialListener();
  },

  async _initialData() {
    // console.log("data");
  },

  _initialListener() {
    // console.log("init");
  },
};

export default Logout;
